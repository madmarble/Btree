
	/* Test block for bitmap*/
	/*
	struct bitmap *bitmap = (struct bitmap *)malloc(sizeof(*bitmap));
	bitmap->init = &init;
	bitmap->init(bitmap, 9);
	bitmap->free = &ffree;
	if (!bitmap->free(bitmap))
		printf("Everything is fine. Ffree works!\n");
	free(bitmap);
	//bitmap->set = &set;
	//int i;
	//for (i = 0; i < 8; i++)
	//	bitmap->set(bitmap, i);
	//bitmap->unset = &unset;
	//for (i = 0; i < 8; i++)
	//	bitmap->unset(bitmap, i);
	//bitmap->first_empty = &first_empty;
	//printf("Test for empty %d\n", bitmap->first_empty(bitmap));
	//bitmap->unset = &unset;
	//bitmap->unset(bitmap, 3);

	//printf("This is bitmap %d\n", bitmap->bitmask[1]);
	*/